

import Vue from 'vue';
// import Wrapper from '..';

describe('Tpl', () => {
	test('测试传递属性', () => {
		expect(1).toBe(1);
		// const component = createComponent(Wrapper, {});
		// expect(typeof component).toBe('object');
	});
});